version: '3'
services:
  db:
    build:
      context: .
      dockerfile: db
    image: 965934840569.dkr.ecr.sa-east-1.amazonaws.com/db
    container_name: db
    networks:
      - bluebank
    ports:
      - '3306:3306'
    volumes:
      - ./docker/volume_mysql:/var/lib/mysql
    restart: always

  api_bluebank:
    container_name: api
    build:
      context: .
      dockerfile: api
    image: 965934840569.dkr.ecr.sa-east-1.amazonaws.com/api
    networks:
      - bluebank
    ports:
      - '8080:8080'
    restart: always
    depends_on:
      - db
networks:
  bluebank:
    driver: bridge